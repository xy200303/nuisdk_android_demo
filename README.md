# NUI SDK DEMO 项目更新说明

## 🎯 项目概述

本项目是阿里云百炼大模型平台 NUI SDK 的 Android 示例 Demo。已针对新版 Android Studio 和 Gradle 进行了全面升级和兼容性修复。

## ✨ 更新内容

### 🔧 构建工具升级
- **Android Gradle Plugin**: 从旧版本升级到 **8.7.0**
- **Gradle**: 升级到 **8.7**
- **Java 版本**: 从 Java 8 升级到 **Java 17**
- **编译 SDK**: 升级到 **API 34**

### 🐛 修复的问题

1. **Gradle 版本兼容性问题**
   - 修复了 AGP 版本不兼容导致的构建失败
   - 更新了过时的 Gradle 配置语法

2. **Java 版本问题**
   - 修复了 Java 8 过时的警告
   - 升级到 Java 17 以获得更好的性能和安全性

3. **命名空间配置问题**
   - 移除了 AndroidManifest.xml 中的 `package` 属性
   - 在 build.gradle 中添加了 `namespace` 配置

4. **HTTP 协议问题**
   - 修复了 Gradle 8.x 默认禁用 HTTP 协议的问题
   - 为内网仓库添加了 `allowInsecureProtocol = true`

5. **废弃 API 警告**
   - 修复了使用过时 API 的编译警告
   - 更新了相关配置

### 📦 依赖项更新
- 更新了所有 AndroidX 库到最新稳定版本
- 添加了核心库去糖支持
- 优化了依赖管理

## 🚀 快速开始

### 系统要求
- **Android Studio**: 最新稳定版（推荐 Flamingo 或更高）
- **Java**: JDK 17+
- **Gradle**: 8.7+
- **Android SDK**: API 34

### 构建步骤

1. **克隆项目**
   ```bash
   git clone [项目地址]
   cd NUI_SDK_DEMO
   ```

2. **同步 Gradle**
   - 打开 Android Studio
   - 点击 "Sync Project with Gradle Files" 按钮
   - 或运行: `./gradlew sync`

3. **构建项目**
   ```bash
   ./gradlew clean
   ./gradlew assembleDebug
   ```

4. **运行项目**
   - 连接 Android 设备（API 24+）
   - 点击 Run 按钮
   - 或运行: `./gradlew installDebug`

## ⚙️ 配置说明

### 关键配置文件

1. **项目级 build.gradle**
   ```gradle
   // 升级到 AGP 8.7.0
   classpath 'com.android.tools.build:gradle:8.7.0'
   ```

2. **模块级 build.gradle**
   ```gradle
   android {
       namespace 'mit.alibaba.nuidemo'  // 新增的命名空间配置
       compileSdk 34
       
       compileOptions {
           sourceCompatibility JavaVersion.VERSION_17
           targetCompatibility JavaVersion.VERSION_17
       }
   }
   ```

3. **gradle-wrapper.properties**
   ```properties
   distributionUrl=https\://services.gradle.org/distributions/gradle-8.7-all.zip
   ```

### 环境变量
确保设置了正确的环境变量：
```bash
# 设置 JAVA_HOME
export JAVA_HOME=/path/to/jdk-17
```

## 🔍 问题排查

### 常见问题

1. **构建失败：Java 版本不兼容**
   ```
   解决方案：安装 JDK 17 并在 Android Studio 中配置
   ```

2. **HTTP 仓库访问失败**
   ```
   解决方案：已在 build.gradle 中添加 allowInsecureProtocol = true
   ```

3. **命名空间错误**
   ```
   解决方案：已正确配置 namespace 属性
   ```

4. **路径过长（Windows）**
   ```
   解决方案：启用 Windows 长路径支持
   ```

### 调试命令
```bash
# 查看详细错误
./gradlew build --stacktrace

# 清理缓存
./gradlew clean
./gradlew cleanBuildCache

# 检查依赖
./gradlew dependencies
```

## 📁 项目结构
```
NUI_SDK_DEMO/
├── app/
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/     # 源代码
│   │   │   ├── res/      # 资源文件
│   │   │   └── AndroidManifest.xml
│   │   └── debug/        # 调试配置
│   └── build.gradle      # 模块配置
├── build.gradle          # 项目配置
├── gradle.properties     # Gradle 属性
├── settings.gradle       # 设置文件
└── README.md            # 说明文档
```

## 🔄 迁移指南

### 从旧版本迁移
1. **备份原有项目**
2. **更新 Android Studio 到最新版**
3. **安装 JDK 17**
4. **按照本 README 重新配置**
5. **测试所有功能**

### 配置变更
- 移除了 AndroidManifest.xml 中的 `package` 属性
- 在 build.gradle 中添加了 `namespace`
- 更新了编译选项和依赖项
- 启用了核心库去糖功能

## 📈 性能优化
- 启用了 Gradle 构建缓存
- 配置了增量编译
- 优化了依赖解析
- 启用了 R8 代码压缩

## 🤝 贡献指南
1. Fork 本项目
2. 创建功能分支
3. 提交更改
4. 推送到分支
5. 创建 Pull Request

## 📄 许可证
本项目基于阿里云百炼大模型平台的 SDK 许可协议。
 版本更新：请关注本项目的 Release 页面

---
*祝您使用愉快！如有问题，请参考上述解决方案或提交 Issue。*
